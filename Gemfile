# Gemfile
source "https://rubygems.org"

# Use the GitHub Pages gem: this pins Jekyll + plugins to exactly
# what GitHub Pages uses in production.
gem "github-pages", group: :jekyll_plugins

# Ruby 3 + Jekyll needs this explicitly
gem "webrick", "~> 1.8"